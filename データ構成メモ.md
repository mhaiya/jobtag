# データ構成メモ（JSON → JS → 表示）

更新日：2026-07-28

## 新しいデータフロー

```
data/*.json  ──fetch──▶  js/*.js  ──描画──▶  HTML
```

データは JS に埋め込まず、すべて `data/` の JSON に集約。各 JS が `fetch()` で読み込んで描画します。

| データ | JSON | 読み込むJS | 表示先 |
|---|---|---|---|
| ランキング（週間/月間/年間・各100件） | `data/ranking-full.json` | `js/ranking.js`（一覧・12件/頁）／`js/main.js`（TOPは上位5件を切り出し） | `ranking.html` / `index.html` |
| 関連サイトバナー | `data/links.json` | `js/main.js` | `index.html` |
| よくある質問 | `data/faq.json` | `js/faq.js` | `faq.html` |

- ランキングは **`ranking-full.json` 一本化**（TOPは上位5件をJS側で切り出し）。旧 `ranking.json` は廃止。
- 旧 `js/ranking-data.js` `js/links-data.js` `js/faq-data.js` は**削除済み**（データはJSONへ移行）。

## データの編集方法

1. 該当する `data/*.json` を編集（項目の追加・削除・並べ替え）。
   - ランキング：`periods.weekly / monthly / yearly` の `items[]`（`rank, title, move, image, url`）。`move` は `up`（上昇）/ `down`（下降）/ `stay`（変動なし）。
   - 関連サイト：`items[]`（`label, image, url`）。
   - FAQ：`sections[].items[]`（`q, a`）。番号は表示時に自動採番（並び順＝番号順）。`a` はHTML可（属性は単引用符 `'…'` 推奨）。
2. ブラウザキャッシュ対策として、対象HTMLの `?v=YYYYMMDD` を1つ進める。

## 画像の置き場所

- データ駆動の写真・バナーは **`img/assets/`** に集約（JSONの `image` はここを指す）。
  現在は `placeholder_photo.svg`（職業写真）・`placeholder_banner.svg`（バナー）を配置。実画像はここへ差し替え。
- ヘッダー/フッター等のUI画像は従来どおり `img/top` `img/ranking` `img/FAQ`。

## 重要：サーバー配信が前提（file:// では動きません）

`fetch()` を使うため、**`index.html` をダブルクリック（file://）で開くと CORS でデータが読めません**。以下のいずれかで確認してください。

- ローカル：フォルダ内で `python3 -m http.server` → `http://localhost:8000/` を開く
- 本番：さくらサーバー等へアップロード（SFTP）

## 画像・データのパス方針（本家との関係）

- 本モックは `.html` がすべて同一階層のフラット構成のため、**相対パス（`img/...` `data/...`）** を採用。
  → ルート配置でもサブディレクトリ配置でも表示され、さくらサーバー検証でも確実に動きます。
- 一方、**本家 job tag は画像をルート絶対パス `/Images/...` で参照**（ページが `/User/`, `/User/Ranking/1` と深さが変わるため）。
  → 将来 Razor アプリへ統合する際は、`img/...` → `/img/...`（またはRazorの `~/` ヘルパー）へ寄せると本家準拠かつ経路の深さに強くなります。この置換だけで移行可能な構成にしてあります。

## レビュー反映（review-share.pdf ／ 2026-07-28）

「職業アクセスランキング 実装レビュー・申し送り」を反映しました。

- **A-1 画像パス**：`img/assets/` を新設して placeholder を集約済み（404解消）。※レビューは `img/top`/`img/ranking` への統一を提案していましたが、事前合意どおり `img/assets/` 集約で対応。
- **A-2 データ一本化**：`ranking-data.js`／`ranking.json` は削除済み、`data/ranking-full.json` の単一ソースを fetch。TOPは上位5件を切り出し。`RANKING_API.url` を実際に使う形にして「URL変更＝接続先変更」の齟齬を解消。
- **A-3 取得失敗時の表示**：ダミーデータのフォールバックを**廃止**し、失敗時は「ランキングを取得できませんでした。」と**明示エラー表示**（TOP/一覧/FAQ）。`console.error` 出力。※ null画像用の `IMG_FALLBACK`（画像だけの差し替え）は仕様どおり維持。
- **A-4 動作環境**：本ドキュメント上部のとおり、確認は HTTP サーバ経由に統一。
- **B-1 var→const/let**：全JSで `var` を全廃（`const`/`let` に統一）。
- **B-2 共通化**：`js/utils.js`（`window.JT`）を新設し `esc()`／`safeUrl()`／`MOVE_LABEL`／`moveHTML()` を集約。全ページで `main.js` より前に読み込み。`safeUrl()` は **相対/ルート相対/フラグメント/https のみ許可**し `javascript:` 等を無害化（B-2 スキーム検証）。FAQ回答 `it.a` は「HTML許容フィールド」とコード上に明記。
- **B-3 既定**：一覧も rank 昇順にソートしてから描画（TOPと統一）。fetch は `cache: 'no-store'`。

### 仕様・バックエンドで決めたい未確定事項（レビュー末尾の確認事項）

1. `move` の比較対象（前週 / 前日バッチ時点）… バックエンド
2. 障害時・データ0件時のレスポンス仕様 … バックエンド（A-3）
3. キャッシュヘッダの設計方針（`Cache-Control: max-age` 等）… バックエンド（B-3）
4. `url` の形式（相対 / https 限定の可否）… バックエンド（B-2で相対・https許可の実装済み）
5. 取得失敗時にエラー表示とする方針で問題ないか … **仕様合意（今回はエラー表示で実装）**
6. `updatedAt`（集計基準日）を画面に表示するか … 仕様
7. 画像パスの統一先 … 実装（`img/assets/` 集約で対応済み）

### バックエンドへ依頼するJSON形式（レビュー案どおり・1エンドポイントで可）

```
{
  "updatedAt": "2026-07-26",
  "perPage": 12,
  "periods": {
    "weekly":  { "label": "週間ランキング", "items": [ /* 100件 */ ] },
    "monthly": { "label": "月間ランキング", "items": [ /* 100件 */ ] },
    "yearly":  { "label": "年間ランキング", "items": [ /* 100件 */ ] }
  }
}
// items[]: { rank:number(1始まり・欠番なし・昇順), title, move:"up"|"down"|"stay", image, url }
```

## 検証済み（2026-07-28・ローカルHTTPサーバー）

- 3ページ / CSS / JS / 3つのJSON / 画像（`img/assets` 含む）すべて HTTP 200。
- 削除した旧ファイルは 404（残骸なし）。
- JSON内の `image` パスはすべて実在ファイルを指す。
- ランキング 100/100/100 件、links 8件、faq 4セクションを確認。JS構文エラーなし。
